# Required: Your Anthropic API key (get from https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Required: Your Arize AI credentials (get from https://app.arize.com/settings/keys)
ARIZE_SPACE_KEY=your-space-key-here
ARIZE_API_KEY=your-api-key-here
OTEL_SERVICE_NAME=your-project-name-here

# Required: LiteLLM master key for proxy authentication and web UI access
# Generate a secure key (e.g., openssl rand -hex 32)
LITELLM_MASTER_KEY=sk-1234-your-master-key-here

# Required: Salt key for encrypting credentials in the database
# IMPORTANT: Cannot be changed once set! Generate a secure key (e.g., openssl rand -hex 32)
LITELLM_SALT_KEY=sk-salt-your-salt-key-here

# Optional: PostgreSQL database password (default: litellm123)
# POSTGRES_PASSWORD=your-secure-password

# Optional: Web UI authentication (leave empty to use only LITELLM_MASTER_KEY)
UI_USERNAME=admin
UI_PASSWORD=password
